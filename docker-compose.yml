version: "3"
services:
  chirpstack:
    image: chirpstack/chirpstack:${VERSION}
    command: -c /etc/chirpstack
    restart: unless-stopped
    volumes:
      - ./configuration/chirpstack:/etc/chirpstack
      - ./lorawan-devices:/opt/lorawan-devices
    depends_on:
      - postgres
      - mosquitto
      - redis
    environment:
      - MQTT_BROKER_HOST=${MQTT_BROKER_HOST}
      - REDIS_HOST=${REDIS_HOST}
      - POSTGRESQL_HOST=${POSTGRESQL_HOST}
    ports:
      - 8080:8080

  chirpstack-gateway-bridge-as923:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1700:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-as923.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge.toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-gateway-bridge-as923_2:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1701:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-as923_2.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge.toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-gateway-bridge-as923_3:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1702:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-as923_3.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge.toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-gateway-bridge-as923_4:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1703:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-as923_4.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge.toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-gateway-bridge-au915_1:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1704:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-au915_1.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge.toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-gateway-bridge-eu868:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1705:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-eu868.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge/toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-gateway-bridge-in865:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1706:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-in865.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge.toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-gateway-bridge-kr920:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1707:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-kr920.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge.toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-gateway-bridge-us915_1:
    image: chirpstack/chirpstack-gateway-bridge:${VERSION}
    restart: unless-stopped
    ports:
      - 1708:1700/udp
    volumes:
      - ./configuration/chirpstack-gateway-bridge/cs-gwb-us915_1.toml:/etc/chirpstack-gateway-bridge/chirpstack-gateway-bridge.toml
      - /etc/localtime:/etc/localtime:ro
    environment:
      - INTEGRATION__MQTT__AUTH__GENERIC__SERVERS=tcp://${MQTT_BROKER_HOST}:1883
      - INTEGRATION__MQTT__AUTH__GENERIC__USERNAME=${MQTT_USER}
      - INTEGRATION__MQTT__AUTH__GENERIC__PASSWORD=${MQTT_PASS}
    depends_on:
      - mosquitto

  chirpstack-rest-api:
    image: chirpstack/chirpstack-rest-api:${VERSION}
    restart: unless-stopped
    command: --server chirpstack:8080 --bind 0.0.0.0:8090 --insecure
    ports:
      - 8090:8090
    depends_on:
      - chirpstack

  postgres:
    image: postgres:14-alpine
    restart: unless-stopped
    volumes:
      - ./configuration/postgresql/initdb:/docker-entrypoint-initdb.d
      - postgresqldata:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=${POSTGRESQL_PASSWORD}

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    volumes:
      - redisdata:/data

  mosquitto:
    image: eclipse-mosquitto:2
    restart: unless-stopped
    ports:
      - 1883:1883
    volumes:
      - ./configuration/mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf

volumes:
  postgresqldata:
  redisdata:

networks:
  default:
    name: core-infra
    external: true
